<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.mldn.dibmp.dao.IStorageApplyDetalisDAO">
	<select id="findPrice" resultType="StorageApplyDetails">
		SELECT said,sum(num) as num,sum(num*price) as price FROM storage_apply_details group by said
	</select>
	<select id="findBySaid" parameterType="Long" resultType="StorageApplyDetails">
		SELECT sadid,said,gid,name,num,price,weight FROM storage_apply_details WHERE said=#{said}
	</select>
	<select id="findNumPrice" resultType="StorageApplyDetails">
		SELECT sum(num) as num,sum(num*price) as price,sum(num*weight) as weight FROM storage_apply_details WHERE said=#{said}
	</select>
</mapper>